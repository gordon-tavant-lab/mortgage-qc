"""The ruleset compiler pipeline (002b) -- scales the compile->correct->sign
mechanism proven in `p0/qc_engine/ruleset.py` and `002a`'s spike to a
production batch. See specs/002b-ruleset-compiler-pipeline/."""
